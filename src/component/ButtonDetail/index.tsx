import React, { ReactNode } from 'react'
import { ButtonWrapper } from './style'

const ButtonDetail = ({ children }: { children: ReactNode }) => <ButtonWrapper>{children}</ButtonWrapper>

export default ButtonDetail
